"use client";import {useMemo,useState} from "react";import {useSearchParams} from "next/navigation";import ProductGrid from "@/components/ProductGrid";import {categories,products} from "@/data/products";import {Search,SlidersHorizontal} from "lucide-react";import {useI18n} from "@/lib/i18n";
export default function ProductsPage(){const params=useSearchParams();const {t,category}=useI18n();const initial=params.get("category")||"All";const [query,setQuery]=useState("");const [cat,setCat]=useState(initial);const [sort,setSort]=useState("featured");const filtered=useMemo(()=>{let list=products.filter(p=>(cat==="All"||p.category===cat)&&((p.name+" "+(p.description||"")).toLowerCase().includes(query.toLowerCase())||(category(p.category).toLowerCase().includes(query.toLowerCase()))));if(sort==="price-low")list=[...list].sort((a,b)=>a.price-b.price);if(sort==="price-high")list=[...list].sort((a,b)=>b.price-a.price);if(sort==="rating")list=[...list].sort((a,b)=>b.rating-a.rating);return list},[query,cat,sort,category]);return <section className="container-x py-14 md:py-20"><div className="max-w-2xl"><span className="text-xs uppercase tracking-[.25em] text-gold">{t("collection")}</span><h1 className="mt-3 text-5xl md:text-7xl">{t("dessertsTitle")}</h1><p className="mt-4 text-cocoa/60">{t("dessertsText")}</p></div><div className="mt-10 rounded-3xl border border-cocoa/10 bg-white/70 p-4 shadow-card"><div className="flex flex-col gap-3 lg:flex-row"><label className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-cocoa/40" size={19}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder={t("search")+"..."} className="w-full rounded-2xl bg-cream py-3.5 pl-11 pr-4 outline-none"/></label><div className="flex gap-3"><select value={cat} onChange={e=>setCat(e.target.value)} className="max-w-[170px] rounded-2xl bg-cream px-4 py-3 outline-none"><option value="All">{t("all")}</option>{categories.map(c=><option key={c} value={c}>{category(c)}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)} className="max-w-[180px] rounded-2xl bg-cream px-4 py-3 outline-none"><option value="featured">{t("featured")}</option><option value="price-low">{t("priceLow")}</option><option value="price-high">{t("priceHigh")}</option><option value="rating">{t("topRated")}</option></select></div></div></div><div className="mt-8 flex items-center justify-between text-sm text-cocoa/55"><span>{filtered.length} {t("productsFound")}</span><span className="inline-flex items-center gap-2"><SlidersHorizontal size={16}/> {t("collection")}</span></div><div className="mt-5"><ProductGrid products={filtered}/></div></section>}