"use client";import Link from "next/link";import ProductGrid from "@/components/ProductGrid";import {useStore} from "@/components/StoreProvider";import {products} from "@/data/products";import {useI18n} from "@/lib/i18n";export default function FavoritesPage(){const {favorites}=useStore();const {t}=useI18n();const list=products.filter(p=>favorites.includes(p.id));return <section className="container-x py-12 md:py-20"><span className="text-xs uppercase tracking-[.25em] text-gold">{t("wishlist")}</span><h1 className="mt-3 text-5xl">{t("wishlistTitle")}</h1>{list.length?<div className="mt-9"><ProductGrid products={list}/></div>:<div className="mt-10 rounded-[34px] bg-white p-12 text-center"><div className="text-5xl">♡</div><h2 className="mt-4 text-3xl">{t("nothingSaved")}</h2><p className="mt-2 text-cocoa/55">{t("saveHint")}</p><Link href="/products" className="mt-7 inline-flex rounded-full bg-cocoa px-6 py-3 font-semibold text-white">{t("exploreDesserts")}</Link></div>}</section>}