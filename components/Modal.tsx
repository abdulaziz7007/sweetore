"use client";import {AnimatePresence,motion} from "framer-motion";import {X} from "lucide-react";export default function Modal({open,onClose,children}:{open:boolean;onClose:()=>void;children:React.ReactNode}){return <AnimatePresence>{open&&<div className="fixed inset-0 z-[100] grid place-items-center bg-cocoa/40 p-4 backdrop-blur-sm"><motion.div initial={{opacity:0,scale:.96,y:12}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.96}} className="relative w-full max-w-lg rounded-3xl bg-white p-6 shadow-soft"><button onClick={onClose} className="absolute right-4 top-4 rounded-full bg-cream p-2"><X size={18}/></button>{children}</motion.div></div>}</AnimatePresence>}