"use client";import {motion,AnimatePresence} from "framer-motion";export default function Toast({message,show}:{message:string;show:boolean}){return <AnimatePresence>{show&&<motion.div initial={{opacity:0,y:15}} animate={{opacity:1,y:0}} exit={{opacity:0,y:15}} className="fixed bottom-5 left-1/2 z-[110] -translate-x-1/2 rounded-full bg-cocoa px-5 py-3 text-sm font-semibold text-white shadow-soft">{message}</motion.div>}</AnimatePresence>}