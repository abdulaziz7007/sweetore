"use client";import {motion} from "framer-motion";export default function Button({children,onClick,type="button",className=""}:{children:React.ReactNode;onClick?:()=>void;type?:"button"|"submit"|"reset";className?:string}){return <motion.button whileTap={{scale:.97}} whileHover={{y:-1}} type={type} onClick={onClick} className={`rounded-full bg-cocoa px-6 py-3 font-semibold text-white ${className}`}>{children}</motion.button>}