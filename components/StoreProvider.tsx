"use client";
import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from "react";
import type {Product} from "@/data/products";
type CartItem=Product&{quantity:number};
type Store={cart:CartItem[];favorites:string[];addToCart:(p:Product,q?:number)=>void;removeFromCart:(id:string)=>void;updateQuantity:(id:string,q:number)=>void;toggleFavorite:(id:string)=>void;isFavorite:(id:string)=>boolean;clearCart:()=>void;subtotal:number};
const StoreContext=createContext<Store|null>(null);
export function StoreProvider({children}:{children:ReactNode}){const [cart,setCart]=useState<CartItem[]>([]);const [favorites,setFavorites]=useState<string[]>([]);const [hydrated,setHydrated]=useState(false);useEffect(()=>{try{const c=localStorage.getItem("sweetora-cart");const f=localStorage.getItem("sweetora-favorites");if(c)setCart(JSON.parse(c) as CartItem[]);if(f)setFavorites(JSON.parse(f) as string[])}catch{}finally{setHydrated(true)}},[]);useEffect(()=>{if(hydrated)localStorage.setItem("sweetora-cart",JSON.stringify(cart))},[cart,hydrated]);useEffect(()=>{if(hydrated)localStorage.setItem("sweetora-favorites",JSON.stringify(favorites))},[favorites,hydrated]);const addToCart=(p:Product,q=1)=>setCart(c=>{const found=c.find(i=>i.id===p.id);return found?c.map(i=>i.id===p.id?{...i,quantity:i.quantity+q}:i):[...c,{...p,quantity:q}]});const removeFromCart=(id:string)=>setCart(c=>c.filter(i=>i.id!==id));const updateQuantity=(id:string,q:number)=>q<=0?removeFromCart(id):setCart(c=>c.map(i=>i.id===id?{...i,quantity:q}:i));const toggleFavorite=(id:string)=>setFavorites(f=>f.includes(id)?f.filter(x=>x!==id):[...f,id]);const subtotal=useMemo(()=>cart.reduce((sum,item)=>sum+item.price*item.quantity,0),[cart]);return <StoreContext.Provider value={{cart,favorites,addToCart,removeFromCart,updateQuantity,toggleFavorite,isFavorite:id=>favorites.includes(id),clearCart:()=>setCart([]),subtotal}}>{children}</StoreContext.Provider>}
export function useStore(){const value=useContext(StoreContext);if(!value)throw new Error("useStore must be used inside StoreProvider");return value;}
