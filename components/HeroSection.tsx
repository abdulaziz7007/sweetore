"use client";import Link from "next/link";import {motion} from "framer-motion";import {ArrowUpRight,Star} from "lucide-react";import {useI18n} from "@/lib/i18n";import {products} from "@/data/products";export default function HeroSection(){const {t,product}=useI18n();const p=products[0];return <section className="container-x grid min-h-[calc(100vh-80px)] items-center gap-10 py-12 lg:grid-cols-[1.02fr_.98fr]"><motion.div initial={{opacity:0,y:25}} animate={{opacity:1,y:0}} transition={{duration:.7}}><span className="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2 text-xs font-semibold uppercase tracking-[.18em] shadow-sm"><Star size={13} fill="currentColor"/> {t("heroEyebrow")}</span><h1 className="mt-6 max-w-2xl text-6xl leading-[.98] md:text-8xl">{t("heroTitle")}</h1><p className="mt-6 max-w-xl text-base leading-8 text-cocoa/60 md:text-lg">{t("heroText")}</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/products" className="inline-flex items-center gap-2 rounded-full bg-cocoa px-7 py-4 font-semibold text-white transition hover:-translate-y-1">{t("shopNow")} <ArrowUpRight size={18}/></Link><Link href="/about" className="rounded-full border border-cocoa/15 bg-white px-7 py-4 font-semibold transition hover:-translate-y-1">{t("ourStory")}</Link></div><div className="mt-10 flex items-center gap-5 text-sm text-cocoa/50"><div className="flex -space-x-2"><span className="h-9 w-9 rounded-full border-2 border-cream bg-rose"/><span className="h-9 w-9 rounded-full border-2 border-cream bg-gold"/><span className="h-9 w-9 rounded-full border-2 border-cream bg-blush"/></div><span><strong className="text-cocoa">{t("happyCustomers")}</strong></span></div></motion.div><motion.div initial={{opacity:0,scale:.96}} animate={{opacity:1,scale:1}} transition={{duration:.9,delay:.1}} className="relative"><div className="absolute -inset-5 rounded-[50px] bg-blush/60 blur-2xl"/><div className="relative overflow-hidden rounded-[44px]"><img src={p.image} alt={product(p).name} className="h-[560px] w-full object-cover md:h-[680px]"/><div className="glass absolute bottom-5 left-5 right-5 rounded-3xl p-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-wider text-cocoa/50">{t("bestseller")}</p><p className="mt-1 font-serif text-2xl">{product(p).name}</p></div><span className="rounded-full bg-cocoa px-4 py-2 text-sm font-semibold text-white">${p.price}</span></div></div></div></motion.div></section>}